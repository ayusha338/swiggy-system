server:
  port: 8082
spring:
  application:
    name: catalog-service
  mongodb:
    uri: mongodb://localhost:27017/catalog_service_db
management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true